// Generate a random password const generatedPassword = Math.random().toString(36).slice(2, 8).toUpperCase(); // Only prints the password when manually called window.getPassword = function () { console.log("Password:", generatedPassword); }; const input = document.getElementById("passwordInput"); const button = document.getElementById("confirmButton"); const error = document.getElementById("error"); function checkPassword() { const enteredPassword = input.value.trim().toUpperCase(); if (enteredPassword === generatedPassword) { localStorage.setItem("passwordPassed", "true"); window.location.replace("/"); } else { error.textContent = "Incorrect password."; input.value = ""; input.focus(); } } button.addEventListener("click", checkPassword); input.addEventListener("keydown", function (event) { if (event.key === "Enter") { checkPassword(); } });
